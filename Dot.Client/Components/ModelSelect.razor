
<MatSelect 
    Outlined="true" 
    Label="Select a model to use"
    Value="@selectedModel"
    ValueChanged="(string i) => OnSelectionChanged(i)"> 
    @foreach (var name in modelNames)
    {
        <MatOptionString Value="@name">@name</MatOptionString>
    }
</MatSelect>